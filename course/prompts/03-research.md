# Video 3 prompt

```text
Use $research-solution to specify how to build docs/requirements/lead-scoring.md.

Inspect the repository and run its baseline tests. Compare deriving scores from activity history with persisting a separate score ledger. Decide where scoring belongs, how list and detail stay consistent, when scores update, how ties sort, and how the prototype is retired. Use $module-design for every changed contract. Write evidence, selected and rejected options, signatures and invariants, a risk register, requirement impact, and open questions to docs/solutions/lead-scoring.md.

Do not write implementation code, delivery units, or commits.
```
