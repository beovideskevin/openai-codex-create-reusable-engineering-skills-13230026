# Video 5 prompts

## Unit 1: Prioritize the lead queue

```text
Use $build to implement Unit 1 from docs/plans/lead-scoring.md.

Stay on the current course branch and do not commit. Name the public scoring contract, write one failing behavior test and run it, add the least production code to pass, and typecheck after green. Replace fake prototype values with the accepted scoring policy, sort the service output deterministically, show real score and tier in the list, run the unit checks, and stop when Unit 1's acceptance condition holds. Record only Unit 1 as complete.
```

## Unit 2: Keep detail consistent

```text
Use $build to implement Unit 2 from docs/plans/lead-scoring.md.

Stay on the current course branch and do not commit. Reuse the existing scored projection, write and run one failing behavior test through the service contract, add the least production code to pass, show the same score and tier in detail, typecheck, run the unit checks, and stop when Unit 2's acceptance condition holds. Record only Unit 2 as complete.
```

## Unit 3: React to a logged reply

```text
Use $build to implement Unit 3 from docs/plans/lead-scoring.md.

Stay on the current course branch and do not commit. Write and run one failing behavior test through the public service contract, add the least production code to pass, and verify a logged reply updates the score and queue immediately. Stop when Unit 3's acceptance condition holds. Then run the full test suite, typecheck, production build, and rendered app, and mark only Unit 3 complete.
```
