# Video 2 prompt

```text
Use $prd to turn BRIEF.md and the Lead Scoring prototype into docs/requirements/lead-scoring.md.

Use the qna discipline to ask exactly one behavior-changing question at a time. Recommend an answer with a short reason. Capture confirmed answers as product behavior, not implementation. The final PRD must include the problem, solution, existing access path, user stories, checkable acceptance criteria, deliberate out-of-scope items, and no unresolved question that would change behavior.

Do not choose modules, storage, file paths, or libraries. Do not commit.
```
