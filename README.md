# Video 4 Begin: Decompose Work into Vertical Slices

You are at the starting state for the planning video. The solution is settled, but it still needs a delivery sequence that proves user behavior early.

## 1. What to do

1. Read the requirement and technical solution.
2. Scan the headings in `slice-work` and optional `to-issues`.
3. Copy `course/prompts/04-slice.md` and invoke `$slice-work`.
4. Make the first unit the thinnest end-to-end scoring path.
5. Check every acceptance criterion in the coverage map.
6. Preview issue publishing only if a tracker and destination are already connected.

## 2. What you should see

- Three dependency-ordered vertical units.
- One public contract, first test, visible proof, and blocker state per unit.
- Complete acceptance-criteria coverage.
- `docs/plans/lead-scoring.md` without production code.

## Follow the video

1. Show why horizontal task lists delay useful feedback.
2. Explain tracer bullets and vertical units as the solution.
3. Introduce `slice-work` and the optional `to-issues` publisher.
4. Show the observable-behavior, unit-anatomy, coverage, and preflight headings.
5. Run `slice-work` against the accepted solution.
6. Inspect unit order, acceptance proof, blockers, and coverage.

Reference result: `course/04-slice-end`.

