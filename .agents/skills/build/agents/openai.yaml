interface:
  display_name: "Build"
  short_description: "Build one planned unit with test-first evidence"
  default_prompt: "Use $build to implement the next planned unit with focused tests and verification."

policy:
  allow_implicit_invocation: false
