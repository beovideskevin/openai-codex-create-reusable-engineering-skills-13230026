interface:
  display_name: "To Issues"
  short_description: "Publish planned units as linked tracker issues"
  default_prompt: "Use $to-issues to publish this plan as dependency-linked issues in my connected tracker."

policy:
  allow_implicit_invocation: false
