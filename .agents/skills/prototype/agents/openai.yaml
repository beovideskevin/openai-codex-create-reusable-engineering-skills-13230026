interface:
  display_name: "Prototype"
  short_description: "Turn a feature idea into a testable UI preview"
  default_prompt: "Use $prototype to create an isolated UI preview for this feature idea."

policy:
  allow_implicit_invocation: false
